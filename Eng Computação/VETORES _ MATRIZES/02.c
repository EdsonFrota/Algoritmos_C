#include<stdio.h>
#include<locale.h>
int main (void)
{


    int i, imp, vetor[100];
    for(i=1; i<=100; ++i)
    {       i=2*i+1;


            printf("\n%d", i);

    }
    getch();
    return 0;
    system("pause");
}
